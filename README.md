# Java Programming Lab – Semester 2 (MCA)

This repository contains Java lab programs for **Semester 2 MCA**.

## Questions / Programs

### Questions 1–14

1. Program to display two numbers using command line arguments, add those numbers, and display the result. (`Program1.java`)
2. Write a program to perform the mathematical operations using switch case. (`Program2.java`)
3. Keep a secret number between 20 and 30 in your program. Ask the user to predict a number between 20 and 30. Use a do-while loop until the user predicts your secret number and display the count of attempts. (`Program3.java`)
4. Implement searching and sorting elements in an array. (`Program4.java`)
5. Implement a Java program to pass an array in different ways. (`Program5.java`)
6. Write a Java program to add and multiply two matrices. (`Program6.java`)
7. A function takes two integer arguments and returns the maximum. Use this function to find the maximum of three numbers. (`Program7.java`)
8. A function takes an integer argument and returns the reverse of the same. Another function takes an integer argument and returns true or false if the number is a palindrome, using the above function. (`Program8.java`)
9. The 15-puzzle (sliding puzzle): rearrange tiles in a 4×4 grid to achieve ascending order (Experiential Learning). (`Program9.java`)
10. Display from **n to 1** and **1 to n** using recursive functions. (`Program10.java`)
11. Create a program for adding, subtracting, multiplying and dividing two numbers using object-oriented concepts. (`Program11.java`)
12. Create a class **Complex** having real and imaginary as data members and provide functions for read, display, add and multiply. (`Program12.java`)
13. Create a class **CString** having a string data member and provide functions for read, display, compare (return Boolean value), add and concatenate. (`Program13.java`)
14. Create a class **Student** having (sno, sname, sprogram). Provide functions for reading and displaying the same information. Also provide a function for comparing two student objects for equality. (`Program14.java`)

> Note: Questions **15–35** are not present in this repository.

### Questions 36 onwards (Official numbering)

36. Program to implement a generic method. (`Program36.java`)
37. Program to implement a generic class. (`Program37.java`)
38. Menu-driven program for string operations:
    - Length of a given string
    - Compare for equality
    - Extract a substring
    - Convert to uppercase and lowercase
    - Reverse a string (`Program38.java`)
39. Program to implement `StringBuffer` class operations. (`Program39.java`)
40. Program to implement server–client communication. (`Program40Server.java`, `Program40Client.java`)
41. Program to implement one-to-one chatting. (`Program41Server.java`, `Program41Client.java`)
42. Client requests a specific filename and downloads its content from the server. (`Program42Server.java`, `Program42Client.java`)
43. Upload file content from client to server. (`Program43Server.java`, `Program43Client.java`)
44. Program to implement public chatting. (`Program44Server.java`, `Program44Client.java`)
45. Program to implement connection-less communication (UDP). (`Program45Server.java`, `Program45Client.java`)
46. Program to get protocol, host, port and path of a given URL. (`Program46.java`)
47. Program to download a file from a given URL. (`Program47.java`)
48. Program to display the content of a webpage given its URL. (`Program48.java`)
49. Program to insert values into a table (PostgreSQL). (`Program49.java`)
50. Program to check if a record exists in a login table (Uname, Upass). (`Program50.java`)
51. Program to display joined data from Emp and Department tables. (`Program51.java`)
52. Program using `PreparedStatement` and `CallableStatement`. (`Program52.java`)
53. Program to implement transaction and batch processing. (`Program53.java`)
54. Program to implement serialization and de-serialization. (`Program54Ser.java`, `Program54DSer.java`, `Student.java`)
55. Program to implement **IS-A** serialization (inheritance with `Serializable`). (`Program55.java`)
56. Program to implement **HAS-A** serialization (composition with `Serializable`). (`Program56.java`)
57. Program to de-serialize selected attributes using `transient` keyword. (`Program57.java`)
58. Program to find maximum of three numbers using AWT. (`Program58.java`)
59. Program to implement all functionality of LIST control in AWT. (`Program59.java`)
60. Program to implement checkbox and radio button in AWT. (`Program60.java`)
61. Program to demonstrate menus using AWT. (`Program61.java`)
